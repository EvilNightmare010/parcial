export enum ContestantStatus {
    ALIVE = 'Alive', // Still in the game
    DEAD = 'Dead', // Killed in battle
    ESCAPED = 'Escaped', // Fled the arena
    FREE = 'Free', // Released from captivity
  }
  